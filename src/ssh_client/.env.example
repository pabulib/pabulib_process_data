###########################################
# SSH Client Configuration                #
###########################################

# Option 1: Use SSH config entry (recommended)
# SSH_CONFIG_HOST=myserver

# Option 2: Direct SSH connection details
# Leave empty to disable SSH features
SSH_HOST=
SSH_PORT=22
SSH_USERNAME=
SSH_PASSWORD=

# Alternative: SSH key authentication (preferred for security)
# SSH_KEY_PATH=/path/to/your/private/key

# Remote directories on the server
SSH_REMOTE_PB_FILES_DIR=/home/pabulib/pb_files
SSH_REMOTE_PB_DEPRECIATED_DIR=/home/pabulib/pb_files_depreciated

###########################################
# Admin upload (inside Docker container)  #
###########################################
# REQUIRED: The Admin UI runs in Docker and reads uploads inside the container.
# Provide the running container name so uploads land in the right place.
SSH_DOCKER_CONTAINER=
# Optional: override container upload path (default below)
SSH_CONTAINER_UPLOAD_TMP_DIR=/tmp/pabulib_uploads
